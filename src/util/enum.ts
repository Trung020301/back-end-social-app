export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum MediaTypeEnum {
  image = 'image',
  video = 'video',
}

export enum CloudinaryResourceTypeEnum {
  image = 'image',
  video = 'video',
  raw = 'raw',
  auto = 'auto',
}

export enum VisibilityPostEnum {
  public = 'public',
  private = 'private',
  followers = 'followers',
}

export enum AccountStatusEnum {
  blocked = 'blocked',
  public = 'public',
  private = 'private',
}

export enum TypeExploreUserEnum {
  my_profile = 'my_profile',
  user_profile = 'user_profile',
}

export enum TypeGenderUserEnum {
  male = 'male',
  female = 'female',
  other = 'other',
}
